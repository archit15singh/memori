name: ci

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder CI
        run: echo "CI will run tests once code is added."
